# TODO

Работаем по этому списку как по основному плану.

## Сейчас в работе

- [x] Показывать время публикации объявления в Telegram-уведомлениях.
- [x] Показывать время публикации в меню `Вакансии`.
- [x] Сохранять `published_at` и `raw_date` в БД для новых лидов.

## Ближайший приоритет (качество потока)

- [ ] Гарантия доставки: очередь отправки в Telegram + retry + пометка `sent_at`.
- [ ] Отдельный статус по площадкам: `ok/error`, `last_success_at`, `found/new`.
- [ ] Авто-детект протухшей сессии по каждой площадке и уведомление в бота.
- [ ] Настройка фильтра из Telegram: `min score`, ключевые слова, минус-слова.
- [ ] Настройка частоты/глубины сканирования из Telegram (`interval`, `max_pages`, `max_leads`).

## Источники и парсинг

- [ ] `YouDo`: обход антибот-защиты (прокси/стабильный browser fingerprint).
- [ ] `Yandex Uslugi`: отдельный сценарий поиска заказов (не главная витрина).
- [ ] Проверка и обновление селекторов по всем площадкам раз в неделю.
- [ ] Единый реестр селекторов с тестом «карточки не меньше N».

## Полуавтомат -> полный автомат

- [ ] Режим «автоотклик с лимитами» (час/день) + стоп-кнопка.
- [ ] Логи ссылок: объявление, отклик, чат, статус отправки.
- [ ] Валидация отклика перед отправкой (анти-спам, релевантность, язык заказчика).

## Инфраструктура

- [ ] Ротация секретов (Telegram/OpenRouter) и хранение только в Railway variables.
- [ ] Ежедневный backup БД.
- [ ] Метрики и алерты (ошибки, 0 новых лидов > X минут, падение воркера).
